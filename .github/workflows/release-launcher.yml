name: Release | Launcher Suite

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  release:
    name: Build (${{ matrix.label }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            label: Linux
          - platform: windows-latest
            label: Windows
          - platform: macos-latest
            label: macOS

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a # v4.2.0
        with:
          node-version: 22
          cache: npm

      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@9399c7bb15dca21d154c549629122736449bd0aa # v1.11.0
        with:
          toolchain: stable
          cache: true

      - name: Install Linux dependencies
        if: matrix.label == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install npm dependencies
        run: npm ci

      # ── CTLauncher (Client) ──────────────────────────────
      - name: Build & Upload — CrystalTides Launcher
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: apps/launcher/client
          tagName: ${{ github.ref_name || inputs.tag }}
          releaseName: 'CTLauncher ${{ github.ref_name || inputs.tag }}'
          releaseBody: |
            ## 🌊 CTLauncher ${{ github.ref_name || inputs.tag }}

            Descarga el **instalador** para tu plataforma y estarás listo.

            | Plataforma | Archivo |
            |------------|---------|
            | Windows    | `CTLauncher-Installer_x64-setup.exe` |
            | Linux      | `CTLauncher-Installer_amd64.AppImage` / `.deb` |
            | macOS      | `CTLauncher-Installer_x64.dmg` |
          releaseDraft: true
          prerelease: false

      # ── CTLauncher Installer ─────────────────────────────
      - name: Build & Upload — CTLauncher Installer
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: apps/launcher/installer
          tagName: ${{ github.ref_name || inputs.tag }}
          releaseName: 'CTLauncher ${{ github.ref_name || inputs.tag }}'
          releaseDraft: true
          prerelease: false

      # ── CTLauncher Uninstaller ───────────────────────────
      - name: Build & Upload — CTLauncher Uninstaller
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: apps/launcher/uninstaller
          tagName: ${{ github.ref_name || inputs.tag }}
          releaseName: 'CTLauncher ${{ github.ref_name || inputs.tag }}'
          releaseDraft: true
          prerelease: false
